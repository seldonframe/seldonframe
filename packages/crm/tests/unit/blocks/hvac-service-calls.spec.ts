// HVAC Service Calls — scaffolded test stubs.
//
// Each test.todo becomes a visible checklist in pnpm test:unit
// output. Replace test.todo with a real test when the
// corresponding tool implementation lands.

import { describe, test } from "node:test";
import assert from "node:assert/strict";

describe("hvac-service-calls — create_service_call", () => {
  test.todo(
    "create_service_call accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});

describe("hvac-service-calls — assign_technician", () => {
  test.todo(
    "assign_technician accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});

describe("hvac-service-calls — complete_service_call", () => {
  test.todo(
    "complete_service_call accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});

describe("hvac-service-calls — list_emergency_queue", () => {
  test.todo(
    "list_emergency_queue accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});
