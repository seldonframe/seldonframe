// HVAC Equipment — scaffolded test stubs.
//
// Each test.todo becomes a visible checklist in pnpm test:unit
// output. Replace test.todo with a real test when the
// corresponding tool implementation lands.

import { describe, test } from "node:test";
import assert from "node:assert/strict";

describe("hvac-equipment — list_equipment_for_customer", () => {
  test.todo(
    "list_equipment_for_customer accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});

describe("hvac-equipment — create_equipment_record", () => {
  test.todo(
    "create_equipment_record accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});

describe("hvac-equipment — update_service_record", () => {
  test.todo(
    "update_service_record accepts a valid args shape and returns the expected returns shape — " +
      "TODO: fill using the pattern in packages/crm/tests/unit/crm-tools.spec.ts",
  );
});
