{
  "$schema": "https://claude.ai/schemas/plugin.json",
  "name": "seldonframe",
  "version": "2.0.0",
  "description": "SeldonFrame Business OS — one command creates a real hosted workspace on *.app.seldonframe.com with CRM, Cal.diy booking, Formbricks intake, and Brain v2. No key, no signup, no setup. Set SELDONFRAME_API_KEY only to unlock Pro capabilities (second workspace, custom domains, full Brain v2).",
  "homepage": "https://seldonframe.com",
  "repository": "https://github.com/seldonframe/seldonframe",
  "author": "SeldonFrame",
  "license": "MIT",
  "keywords": ["crm", "business-os", "booking", "intake", "brain"],
  "mcpServers": {
    "seldonframe": {
      "command": "node",
      "args": ["skills/mcp-server/src/index.js"],
      "env": {
        "SELDONFRAME_API_KEY": "",
        "SELDONFRAME_API_BASE": ""
      }
    }
  },
  "env": {
    "SELDONFRAME_API_KEY": {
      "description": "Optional. The first workspace is free forever with no key. Set this only to unlock Pro capabilities: second workspace, custom domains, full Brain v2, publishing. Get a key at https://app.seldonframe.com/settings/api.",
      "required": false
    },
    "SELDONFRAME_API_BASE": {
      "description": "Optional override for the Seldon API base URL. Defaults to https://app.seldonframe.com/api/v1.",
      "required": false
    }
  }
}
