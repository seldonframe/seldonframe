# content-loop run — 2026-07-15

**Outcome: CIRCUIT BREAKER.** 1 article drafted and gate-passed (below the 3-article floor). Pushed to `chore/content-loop-2026-07-15` as a draft PR — not merged. Auto-publish forfeited for this run.

## Preconditions

- `DATAFORSEO_AUTH_B64` present in `packages/crm/.env.local` and the account was live-verified at run start: a `keyword_ideas/live` call for "ai receptionist" returned `status_code 20000` (cost $0.01236).
- `gh auth status` — logged in as `seldonframe-devbox`, valid token.
- Guides engine present on `origin/main` (`packages/crm/src/lib/seo/guides/index.ts`, `packages/crm/src/lib/seo/guide-markdown.ts`).

All three passed. Proceeded.

## Step 0 — Commissioned queue

`docs/ops/agents/content-queue.md` had exactly one `status: queued` entry: **"Is GEO legit? (the practitioner's answer)"** (commissioned 2026-07-09 by Max). Drafted it in full — see below. Flipped its status to `shipped: 2026-07-15 is-geo-legit` in this same branch.

## Step 1 — Research (what actually happened)

No fresh `docs/strategy/keyword-recon/*.md` queue exists (directory doesn't exist at all — Phase-1 keyword recon has not produced one yet).

Fell back to seeding `dataforseo_labs/google/keyword_ideas/live` from cluster topics + query patterns per the runbook. Three calls were made this run:

1. `keyword_ideas/live`, seed = `["ai receptionist"]`, limit 3 — precondition verification call. Cost $0.01236. Returned real data (confirms account was live at that point).
2. `keyword_ideas/live`, seeds = 8 mixed full-sentence questions across clusters (no-shows, ai-visibility, reviews, lead-qualification, white-label pricing, ai-receptionist), limit 80. Cost $0.0216. **Result was unusable** — the mixed diverse seeds returned generic off-topic "how to X" ideas (e.g. "how to write a check" at 165k/mo, "apa in-text citation" at 110k/mo, "how to write a cover letter") with zero relevance to the SeldonFrame niche. None of the 80 returned items were usable candidates.
3. `keywords_data/google_ads/search_volume/live`, 30 hand-crafted long-tail operational-question keywords (e.g. "how to reduce no-shows", "why is my business not showing up in chatgpt"). Cost $0.09 — this endpoint is priced far higher per call than the `dataforseo_labs` equivalents. Nearly every keyword returned `search_volume: None` (too long-tail / no measurable volume); only 3 had any recorded volume (10/mo each — not attainable).

A fourth call (targeted, single-cluster niche seeds: "appointment no show rate", "google review request text", "ai lead qualifier", "white label ai pricing", "ai receptionist cost") returned **HTTP 402 Payment Required**. Checked the account balance via the free `appendix/user_data` endpoint: **balance is -$0.0237** — the account ran out of funds mid-run, pushed negative by the cumulative cost of the three calls above (~$0.124 total against a starting balance that was apparently already close to zero).

**No further live DataForSEO calls were possible.** Per the runbook's own rule — "Never fabricate a volume — if a call fails, say so and work from the keyword-recon queue" — and given no keyword-recon queue exists as a fallback, no additional keywords could be honestly researched or volume-validated this run.

## Step 1.5 — Information gain

Not reached. No researched candidates survived Step 1 to need it.

## Step 2 — Dedupe + plan

Only the commissioned piece was planned. Deduped `is-geo-legit` against the existing `ai-visibility` cluster (`what-is-generative-engine-optimization`, `local-seo-vs-geo`, `what-is-answer-engine-optimization`, `how-to-show-up-in-ai-search`, `how-to-get-your-business-recommended-by-chatgpt`) — none target the "is GEO legit / hype vs real" skeptic intent this piece owns. No overlap.

## Step 3 — Drafted

**`is-geo-legit`** — "Is GEO Legit, or Just SEO Rebranded? A Practitioner's Answer"
- Cluster: `ai-visibility` · relatedTool: `/tools/ai-visibility-checker` · relatedChart: `/charts/ai-recommendation-index`
- Thesis: GEO is ~90% ordinary SEO plus a genuinely-different ~10% (citability: Markdown twins, sources rows, verified dates, author entity). Includes an honest caveat that Google's own AI-optimization guide states `llms.txt` doesn't affect Google Search rankings either way — our own practice doesn't universally help, and the piece says so.
- Sources (both WebFetch-verified before citing): Google Search Central "Optimizing your website for generative AI features on Google Search"; Aggarwal et al. "GEO: Generative Engine Optimization" (arXiv 2311.09735, KDD 2024).
- Files added: `packages/crm/src/lib/seo/guides/is-geo-legit.ts`, `packages/crm/src/app/guides/is-geo-legit.md/route.ts`; wired into `packages/crm/src/lib/seo/guides/index.ts`.

## Step 4 — Quality gate

Self-critique: passed (citable claims, no filler, distinct intent, not machine-spun — see reasoning in the commit).

Mechanical gate:
- `npx tsx --test tests/unit/seo/guides.spec.ts` — **870/870 pass.**
- `pnpm typecheck` — clean except the pre-existing, ignorable `src/app/api/copilot/turn/route.ts` error.
- `pnpm build` — **exits 0**, `/guides/is-geo-legit` and `/guides/is-geo-legit.md` present in the route manifest.

**Circuit breaker triggered:** only 1 article total survived this run (the commissioned piece) — below the 3-article floor. Per the runbook, auto-publish is forfeited even though the gate is green.

## What published

Nothing. Draft PR only.

## What was dropped and why

- The entire researched batch (target: up to 14 more articles): dropped because DataForSEO couldn't produce volume-validated, on-topic candidates this run — one call returned only irrelevant generic keywords, a second (expensive) call returned mostly zero-volume long-tail phrases, and the account then ran out of balance before a better-targeted query could run. Nothing was fabricated to fill the gap.

## Next run

Two things need to happen before next week's loop can publish a normal-sized batch:
1. **Top up the DataForSEO account balance** — it's currently negative.
2. **Build the Phase-1 keyword-recon queue** (`docs/strategy/keyword-recon/*.md`) so the loop has a non-live-API fallback, and prefer `dataforseo_labs/google/keyword_ideas/live` with **single-cluster, narrow compound-phrase seeds** (2-4 words, not full question sentences, not mixed across clusters) — this run's data shows mixed multi-cluster seeds and full-sentence seeds both produce unusable results, while `keywords_data/google_ads/search_volume/live` is ~4-7x the cost per call of the `dataforseo_labs` equivalents for the same information.

Commissioned queue: 1 of 1 remaining `queued` entries shipped (`is-geo-legit`). No commissions remain queued.
