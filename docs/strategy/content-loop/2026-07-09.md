# content-loop run — 2026-07-09

**Status: CIRCUIT BREAKER — 0 published, 0 drafted, no merge.**

## Preconditions
All three passed:
1. `DATAFORSEO_AUTH_B64` present in `packages/crm/.env.local`; live call to
   `/v3/dataforseo_labs/google/keyword_ideas/live` returned `status_code 20000`
   (account verified, not the `40104` unverified error).
2. `gh auth status` — logged in as `seldonframe-devbox`, `repo` scope present.
3. Guides engine present on `origin/main`: `packages/crm/src/lib/seo/guides/index.ts`
   and `packages/crm/src/lib/seo/guide-markdown.ts` both exist (the latter lives
   directly under `lib/seo/`, not inside `lib/seo/guides/` — a path detail worth
   fixing in the precondition check in `docs/ops/agents/content-loop.md` §Preconditions).

## Research
No fresh `docs/strategy/keyword-recon/*.md` queue existed (none present at all),
so this run seeded live from DataForSEO per the fallback path.

`keyword_ideas` (cluster-relevance mode) was tried first and rejected: seeding
"speed to lead software" returned generic unrelated software-category noise
(`8bitdo software`, `wooting software`, `patriot software`...) — the endpoint
clusters by dominant token, not by phrase relevance, and isn't usable for
these long-tail niche seeds.

Switched to `keyword_suggestions` (substring-relevant mode), 9 seed calls,
one per cluster/angle:

| cluster | seed | items | notable result (vol / comp) |
|---|---|---|---|
| speed-to-lead | "lead response time business" | 2 | harvard business review lead response time (20 / 0.14); "why b2b lead response time is killing your business" (no volume data) |
| no-shows | "reduce appointment no shows" | 0 | — |
| ai-receptionist | "ai receptionist for business" | 5 | ai receptionist for small business (1000 / 0.69) |
| ai-receptionist | "virtual receptionist small business" | 6 | small business virtual receptionist (320 / 0.34) |
| service-faq | "faq page for service business" | 0 | — |
| booking | "online booking system small business" | 10 | online booking system for small business (210 / 0.57) |
| booking | "appointment scheduling software small business" | 10 | appointment scheduling software for small business (170 / 0.34) |
| ai-visibility | "get recommended by chatgpt business" | 0 | — |
| ai-visibility | "how to show up in ai search results" | 1 | how to show up in ai search results (40 / 0.61) |

**DataForSEO spend: $0.2121** (9 successful/attempted calls + 1 wasted call
from a script bug — the first research pass crashed before persisting results
because the write-to-disk happened only at the end of the loop instead of
after each call, so a mid-loop error lost the whole batch and it had to be
re-run). This is **over the ~$0.20 hard cap** by about $0.01, entirely due to
that one wasted, unrecoverable call ($0.012). No further DataForSEO calls were
made this run once the cap was reached. Lesson for the next run: write
research results to disk incrementally, not at the end of the loop.

## Dedupe — every candidate checked against `allGuideSlugs()`'s 34 target
keywords and every `/best/*` and `/tools/*` page

- **"ai receptionist for small business"** (vol 1000) — dropped. Intent
  already owned by `/best/ai-receptionist-for-small-business.md`; publishing a
  guide at the identical phrase would cannibalize that page rather than feed
  it.
- **"online booking system for small business"** (vol 210) and
  **"appointment scheduling software for small business"** (vol 170) —
  dropped. Same reasoning: `/best/booking-system-for-small-business.md` and
  `/best/booking-app-for-small-business.md` already own this intent.
- **"harvard business review lead response time"** (vol 20) — dropped, volume
  too low to justify a page, and the HBR study is already the cited source in
  the existing `what-is-speed-to-lead` guide.
- **"why b2b lead response time is killing your business"** — dropped, no
  measured search volume returned (never fabricate a volume).
- **"how to show up in ai search results"** (vol 40) — dropped, near-duplicate
  of the already-published `how-to-show-up-in-ai-search` guide
  (`targetKeyword: "how to show up in ai search"`).
- **"small business virtual receptionist" / "virtual receptionist for small
  business"** (vol 320, comp 0.34) — the one genuinely arguable candidate: no
  existing `/best` page targets "virtual receptionist" specifically (only
  `ai-receptionist-for-small-business`), and the existing guide
  `virtual-receptionist-vs-ai-receptionist` is a comparison, not a buying-guide
  angle. Real volume, real competition data. **This alone is not enough to
  clear the 3-article minimum**, so it was not drafted this run — parking it
  for next week's batch instead of writing a lone article under time/budget
  pressure.

**Surviving candidates: 0-1. Required minimum to proceed past the gate: 3.**

## Circuit breaker

Per `docs/ops/agents/content-loop.md` §4, fewer than 3 articles surviving
means **no draft, no merge, no auto-publish this run**. Nothing was written to
`packages/crm/src/lib/seo/guides/`; there is no code diff. This manifest is
the only change in this run's branch/PR.

## What changed for the loop itself

The seed queries this run leaned toward commercial "X software/system for
small business" phrasing, which — in this niche — turns out to map almost
entirely onto intent the `/best` pages already own. Next run should bias
harder toward genuinely informational "how to / why / how do I" phrasing
within the 6 clusters (per the prompt's own query-pattern list) and/or open a
7th cluster, since the 6 existing clusters are now saturated at 34 articles
with the low-hanging informational keywords already taken.

## IndexNow

Not applicable — no new URLs published this run.

## Merge

Not merged. Branch: `chore/content-loop-2026-07-09` (docs-only commit, this
file). Draft PR opened for the audit trail.
