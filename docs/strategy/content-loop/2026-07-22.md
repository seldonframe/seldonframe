# content-loop run — 2026-07-22

**Result: CIRCUIT BREAKER — not merged.** Base commit: `5ff9c99b`. Branch: `chore/content-loop-2026-07-22`.

## Preconditions

- `DATAFORSEO_AUTH_B64` present, and a live call to `dataforseo_labs/google/keyword_ideas/live` returned `status_code 20000`. ✅ (technically satisfied — see Step 1 below for what happened next).
- `gh auth status`: logged in as `seldonframe-devbox`, scopes `read:org repo`. ✅
- Guides engine present on `origin/main`: `guides/index.ts` ✅, `src/lib/seo/guide-markdown.ts` ✅ (same note as 2026-07-10: one level up from the path the runbook names, same engine).

## Step 0 — commissioned queue

One `queued` entry: **"Is GEO legit? (the practitioner's answer)"** (`is-geo-legit`). Drafted and shipped this run; flipped its status to `shipped: 2026-07-22 is-geo-legit` in `docs/ops/agents/content-queue.md`.

## Step 1 — research: BLOCKED (DataForSEO account balance negative)

The precondition's single `keyword_ideas/live` call succeeded, but it apparently spent the account's last available credit. Checked `/v3/appendix/user_data` immediately after: **account balance is -$0.0237** (negative). Every subsequent research call — `keywords_data/google_ads/search_volume/live` (an 18-keyword hand-built batch, informed by the 2026-07-10 run's "batch bigger, avoid the per-call floor" lesson) and `dataforseo_labs/google/keyword_overview/live` (same batch) — returned `40200 Payment Required`.

Per the runbook's own instruction ("never fabricate a volume — if a call fails, say so and work from the keyword-recon queue"): no `docs/strategy/keyword-recon/` queue exists (same as 2026-07-10), so there was no fallback data to research from. **No paid calls were attempted beyond the precondition check and the two failed batches** (both returned before any cost was incurred, per the `cost: 0` field on the failed tasks) — total DataForSEO spend this run: **$0** beyond whatever the one successful precondition call cost (reflected in the -$0.0237 balance, likely carried over from a prior run/precondition check, not newly spent here).

I did not attempt to add funds to the DataForSEO account — that's a billing action outside this agent's authority; it needs a human decision.

**Consequence:** zero researched (non-commissioned) candidates this run. The only candidate keyword list built (18 terms spanning a real content gap — A2P 10DLC compliance, which has no supporting guide despite an existing free `/tools/a2p-10dlc-checker` tool, plus a Klaviyo-cost angle paralleling the GoHighLevel-cost guides) is preserved below for next run so the research isn't wasted:

| Keyword | Why it looked promising |
|---|---|
| what is a2p 10dlc | `/tools/a2p-10dlc-checker` exists with zero supporting guide content — real gap |
| a2p 10dlc registration | same gap, transactional angle |
| a2p 10dlc cost | same gap, commercial angle |
| how to register for a2p 10dlc | same gap, how-to angle |
| 10dlc compliance | same gap, compliance angle |
| klaviyo pricing / how much does klaviyo cost | `/tools/klaviyo-cost-calculator` exists with no guide, parallel to the well-populated `how-much-does-gohighlevel-cost` pattern |
| klaviyo alternative for small business / klaviyo vs mailchimp | same tool, alternative/comparison angle |

None of these have measured volume yet — that's exactly what next run's `search_volume` or `keyword_overview` call should confirm once the account has a balance.

## Step 1.5 — information gain

Not invoked for `is-geo-legit`. Reasoning: the live SERP for this topic (checked via WebSearch, not DataForSEO) is dominated by agency-blog listicles restating the same "GEO is real, here's a checklist" points — genuinely commodity. But the information-gain skill mines *niche YouTube transcripts* for founder stories; this piece's actual differentiator is stronger and more directly relevant than any transcript would be: our **own instrumented practice** (the AI Recommendation Index methodology, including the honest disclosure that SeldonFrame itself is absent from the July 2026 snapshot) is primary-source, verifiable, and exactly on-topic in a way a third-party video couldn't be. Used that instead.

## Step 3 — drafted (1 article, the commission)

| Slug | Target keyword | Cluster | relatedTool | Sources |
|---|---|---|---|---|
| `is-geo-legit` | is geo legit | ai-visibility | `/tools/ai-visibility-checker` | Adobe Semrush-acquisition press release, llmstxt.org spec, Gulf News (OpenAI AI-classifier shutdown), arXiv 2311.09735 (GEO paper, cross-linked rather than re-explained) |

All four external sources were fetched and verified with WebFetch before citing (the arXiv abstract page, Adobe's own press release, llmstxt.org, and Gulf News's coverage of OpenAI's classifier shutdown — the OpenAI blog post itself returned HTTP 403 to WebFetch, so a secondary source that quotes it directly was used instead, consistent with the never-lies rule of only citing what actually resolves). The `/charts/ai-recommendation-index` page (our own, already-published, verified-in-place methodology and the SeldonFrame-absence disclosure) is linked in-body and via `relatedChart` rather than listed as an external source.

**Dedupe check:** this cluster already has 4 guides (`what-is-generative-engine-optimization`, `what-is-answer-engine-optimization`, `local-seo-vs-geo`, `how-to-show-up-in-ai-search`). `what-is-generative-engine-optimization` in particular already covers the Princeton/KDD GEO paper and the "Google says it's still SEO" framing in real depth. `is-geo-legit` deliberately does NOT re-explain that paper — it cross-links to the sibling guide for depth and instead leans on material none of the four existing guides cover: our own citability infrastructure (md twins, llms.txt, sources rows), the "sellers oversell" debunk (em-dash scrubbing, AI-detection-proofing, magic-prompt hacks — backed by the OpenAI classifier-shutdown source), and the Adobe/Semrush acquisition as a market-legitimacy signal.

**Practitioner quotes:** checked `~/agents/quotes-library.md` — no entries relevant to GEO/AI-visibility. None used, per the skip-when-nothing-fits rule.

## What was dropped and why

- **The entire non-commissioned research batch** — blocked by the DataForSEO account's negative balance (see Step 1). Not a quality judgment; simply never measured. The 18-keyword candidate list above (A2P 10DLC compliance angle + Klaviyo-cost angle) is preserved for next run rather than discarded.

## Mechanical gate

```
npx tsx --test tests/unit/seo/guides.spec.ts   → 870/870 pass
pnpm typecheck                                  → 0 new errors (pre-existing copilot/turn/route.ts error only)
pnpm build                                      → FAILS: "Module not found: Can't resolve '@seldonframe/reelier/skill'"
```

The build failure is **pre-existing and unrelated to this run's content changes** — verified by `git stash`-ing all of this run's edits and re-running `pnpm build` against unmodified `origin/main` (`5ff9c99b`): identical failure. `@seldonframe/reelier` (pinned to `0.2.0` in `packages/crm/package.json` since PR #142) is simply absent from `node_modules` in this environment, and there is no `.npmrc`/registry token anywhere on the box to install it. This is an environment/infra gap for engineering to fix (likely a private-registry auth token that needs provisioning on this devbox), not a content-loop concern — flagging it here rather than attempting to patch registry config with no credentials.

## Circuit breaker

Both trigger conditions are met independently: **the build fails**, and **only 1 article** (the commission) was produced — below the 3-article floor, because DataForSEO's account balance blocked the researched batch entirely. Per the runbook: **auto-publish is forfeited.** Work pushed to `chore/content-loop-2026-07-22` and opened as a **draft** PR instead of merged.

## Next run's plan

1. **Top up the DataForSEO account before the next run** (human decision — not something this agent will do unilaterally). Balance was -$0.0237 at check time.
2. **Provision registry credentials for `@seldonframe/reelier`** on this devbox (or otherwise fix `pnpm install` for that package) so `pnpm build` passes again — this blocks every future content-loop run, not just this one.
3. Once both are fixed, the 18-keyword A2P-10DLC / Klaviyo-cost candidate list above is ready to measure immediately — no research work needs to be redone.
