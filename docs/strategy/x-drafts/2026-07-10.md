# X drafts — 2026-07-10

Cadence memory: first drafts file. Formats used today: build log · contrast · value.
Receipt series (GSC/Bing graph): not yet started, anchor to real data when it exists.

---

## Draft 1 — build log (no keyword)
Vault patterns applied: self-surprise build log (#10), one number with context (#1)

> Told my agent "pull my X bookmarks" and walked away.
>
> It opened the X dev console in my browser, set up the OAuth app, clicked
> through the consent screen, caught the callback on localhost, and pulled all
> 50 bookmarks into a markdown file.
>
> I thought the API setup would eat my evening. It took the agent 15 minutes,
> and I never touched a key.
>
> Next: it mines the bookmarks for why each post performed, and drafts my posts
> from what actually works. The content loop feeds itself now.

**Visual:** terminal screenshot of `node scripts/x-bookmarks-pull.mjs` output
("Authorized as @themaxthule … Appended 50 new bookmark(s)"). Take it fresh —
crop out nothing, the plainness is the receipt.

---

## Draft 2 — contrast hook (no keyword)
Vault patterns applied: contrast resolves in the reader's head, visual IS the post (#9)

> I doomscroll X and bookmark what stops me.
>
> My agent reads the bookmarks, works out why each one performed, and writes my
> drafts using those exact mechanics.
>
> My procrastination is now my content strategy.

**Visual:** screenshot of the vault's Patterns section (the 11 numbered lines in
docs/strategy/x-vault.md) — blur nothing, the specificity sells it.

---

## Draft 3 — value post (keyword: Claude Code)
Vault patterns applied: replace-the-tool frame (#4), steps with the why

> Claude Code replaced my content tool. Here's the loop:
>
> 1. Bookmark posts that stop your scroll. That's the whole capture step — if
> it costs effort you'll quit in a week.
>
> 2. Agent pulls them weekly via the X API and logs metrics. Views and replies
> tell you what the market rewards, not what you like.
>
> 3. Agent distills each into one line: hook mechanic, format, why it worked.
> Structure is learnable. Voice isn't — that stays yours.
>
> 4. When you ship something, the agent drafts posts using those patterns plus
> your real numbers. Evidence in, drafts out.
>
> 5. Your own post metrics feed back into the file. The loop learns your
> account, not someone else's.
>
> One markdown file. No subscription.

**Visual:** none needed, the list carries it. Optional: side-by-side of a raw
bookmark entry vs its distilled pattern line.

---

## Creatives + paste-ready (added same day)

### Draft 1 — CAPTURE (receipt; do not generate)
Screenshot your real terminal: run `node scripts/x-bookmarks-pull.mjs` fresh, capture
the window showing the command + "Authorized as @themaxthule" + "Appended N new
bookmark(s)". Crop to the terminal only, dark theme as-is, no beautifying.
Goes as image 1 (the only image).

**PASTE-READY (image 1 = terminal capture):**
```
Told my agent "pull my X bookmarks" and walked away.

It opened the X dev console in my browser, set up the OAuth app, clicked through the consent screen, caught the callback on localhost, and pulled all 50 bookmarks into a markdown file.

I thought the API setup would eat my evening. It took the agent 𝟭𝟱 𝗺𝗶𝗻𝘂𝘁𝗲𝘀, and I never touched a key.

Next: it mines the bookmarks for why each post performed, and drafts my posts from what actually works. The content loop feeds itself now.
```

### Draft 2 — GENERATED (done, ready to attach)
`docs/strategy/x-creatives/2026-07-10/draft2-vault-loop-card.png` (2400x1350,
rendered from the .html next to it — edit HTML + re-run
`node scripts/x-creative-shot.mjs <html>` to tweak). Goes as image 1.

**PASTE-READY (image 1 = vault-loop card):**
```
I doomscroll X and bookmark what stops me.

My agent reads the bookmarks, works out why each one performed, and writes my drafts using those exact mechanics.

𝗠𝘆 𝗽𝗿𝗼𝗰𝗿𝗮𝘀𝘁𝗶𝗻𝗮𝘁𝗶𝗼𝗻 𝗶𝘀 𝗻𝗼𝘄 𝗺𝘆 𝗰𝗼𝗻𝘁𝗲𝗻𝘁 𝘀𝘁𝗿𝗮𝘁𝗲𝗴𝘆.
```

### Draft 3 — no image (the list carries it)
Note the first line stays PLAIN text on purpose: it's the keyword line ("Claude
Code") and Unicode bold chars break keyword indexing — never bold the keyword line.

**PASTE-READY (no image):**
```
Claude Code replaced my content tool. Here's the loop:

1. Bookmark posts that stop your scroll. That's the whole capture step. If it costs effort you'll quit in a week.

2. Agent pulls them weekly via the X API and logs metrics. Views and replies tell you what the market rewards, not what you like.

3. Agent distills each into one line: hook mechanic, format, why it worked. Structure is learnable. Voice isn't. That stays yours.

4. When you ship something, the agent drafts posts using those patterns plus your real numbers. Evidence in, drafts out.

5. Your own post metrics feed back into the file. The loop learns your account, not someone else's.

𝗢𝗻𝗲 𝗺𝗮𝗿𝗸𝗱𝗼𝘄𝗻 𝗳𝗶𝗹𝗲. 𝗡𝗼 𝘀𝘂𝗯𝘀𝗰𝗿𝗶𝗽𝘁𝗶𝗼𝗻.
```

---

Pick one, adjust the angle, post. Repost winners in 6-8 weeks with updated
numbers; quote-tweet anything that runs.
