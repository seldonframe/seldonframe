# T1 — Wedge adoption (positioning + 10-close sprint + freeze list)

**Decision:** the 3 opens from docs/strategy/2026-07-07-wedge-to-1m-arr.md §7.
**Stakes:** the $1M ARR path vs $0 external revenue — the biggest open decision
in the company. Each week of sprint delay ≈ pushes the month-6 ~$23k MRR
scenario a week right.

**First card (pre-disagreement-contract):** "adopt all three, sprint = priority
#1" — agreed with the doc's own recommendation. Flagged by Max the same day as
consultant-grade; rerun under the beat-it rule below.

**The beat-it attack (strongest case against "sprint now"):** closing 10 locals
TODAY sells a weakened offer — A2P is still blocked (SMS text-back = half the
receptionist's value) and the ROI counter (rung 3) doesn't exist, so early
churn poisons the case studies and G2 reviews the listing blitz depends on.
The doc's own ladder gates rung 2 behind rung 1 — and rung 1 (a ~30-minute A2P
resubmission with a known fix) has been pending 8 days while ~15 features
shipped. That is the building-displaces-selling pattern (PATTERNS.md) wearing
a strategy costume.

**Final call (survived the attack, changed by it):** adopt all three — BUT the
sprint is not "priority #1" as an abstraction; the priority is the ladder's
actual first rung: A2P resubmission TODAY, the 10-prospect list named within
48h (the 9 demo verticals are the seed), first close attempt within 14 days
via /start. Voice-only closes may start before A2P clears (weakened, honest
offer) rather than waiting. The freeze list gets teeth: new-surface starts get
logged in LOG.md like any decision, so violations are visible instead of vibes.
Positioning sentence goes into CLAUDE.md §1b now — it's already de facto true.

**Flips if:** the weekly funnel report shows ungated-build converting >~2%
built→paid (path A re-weights); A2P blocked 2+ more weeks (offer math changes);
Max can't name 10 prospects (week 1 becomes prospecting, timeline shifts).

**Max's final call:** pending — recorded here when made (divergence = data).
