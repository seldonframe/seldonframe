# Checkout Reconciliation — $29/mo Flat + 14-Day Trial + 2% GMV — Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Steps use `- [ ]`. **PAYMENT-SENSITIVE — do NOT merge; the branch is handed to Max to set up Stripe + test in test mode first.** No migration (env + code only).

**Goal:** Make checkout match the site: **$29/mo flat · unlimited workspaces · 14-day free trial**, and take a **2% GMV application fee** on payments processed through SeldonFrame's Stripe Connect.

**Architecture (from recon):** Reuse the existing tier machinery — the **"agency" tier is already `maxOrgs: -1` (unlimited)**, so point it at a new **$29** price and drop the per-workspace overage → that becomes the single offered plan. Add `trial_period_days: 14` to the platform checkout. For GMV, add a **2% application fee** at the three connected-account SMB-sales charge sites (a fee % for subscriptions, a computed amount for invoices). The soul-marketplace charge (`application_fee_amount: 0`) is a SEPARATE policy decision — leave it and flag it.

**Tech Stack:** Next.js 16, Stripe SDK. Conventions: tests `cd packages/crm && node --import tsx --test <files>`; tsc 0-new (~10 `.next/types` baseline); `bash scripts/check-use-server.sh src` (run from `packages/crm`); TDD the pure helpers; commit per task. Recon: `lib/billing/plans.ts`, `price-ids.ts`, `app/api/stripe/checkout/route.ts`, `lib/proposals/checkout.ts`, `lib/payments/providers/stripe.ts`.

---

## Task 1: One $29/mo flat plan + 14-day trial

**Files:** `packages/crm/src/lib/billing/plans.ts`, `price-ids.ts`, `app/api/stripe/checkout/route.ts`, `app/api/v1/billing/claim-and-checkout/route.ts`.

- [ ] **Step 1 — the plan catalog.** In `plans.ts` (the **agency** tier, ~164–192): set the display **price to `29`**, `limits.includedWorkspaces: -1` (no overage), `limits.maxOrgs: -1` (already unlimited). Keep `stripePriceId: AGENCY_BASE_PRICE_ID`. Update the plan's display name/blurb to "$29/mo · unlimited workspaces" (this is now the single offered plan). Leave `builder`/`workspace` tiers in the catalog so legacy subscriptions still resolve in the webhook, but they are no longer offered.
- [ ] **Step 2 — the price id.** In `price-ids.ts` (~55–58): `AGENCY_BASE_PRICE_ID` reads env `STRIPE_AGENCY_BASE_PRICE_ID`; change the **fallback** placeholder from `price_PLACEHOLDER_agency_297` → `price_PLACEHOLDER_flat_29`. (Max sets the real `STRIPE_AGENCY_BASE_PRICE_ID` to the new $29 price — see Stripe steps.) Make `AGENCY_WORKSPACE_OVERAGE_PRICE_ID` optional/unused (the $29 plan has no overage); if `checkout-items.ts`/`handlers.ts` reference it, guard so a missing overage id is a no-op (don't throw).
- [ ] **Step 3 — the 14-day trial.** In `app/api/stripe/checkout/route.ts` (the `stripe.checkout.sessions.create` at ~225 — the PLATFORM subscription, NOT a connected account): add `trial_period_days: 14` inside `subscription_data`. Do the same in `app/api/v1/billing/claim-and-checkout/route.ts` (~248). (The legacy `lib/billing/actions.ts:73` already sets it.)
- [ ] **Step 4 — tier resolution sanity.** Confirm `lib/billing/tier-resolve.ts` resolves the org's tier from the price id on the live subscription (data-driven) → the new $29 price (env) maps to the agency/`unlimited` tier. If it hardcodes the old `$297` id anywhere, point it at `AGENCY_BASE_PRICE_ID`.
- [ ] **Step 5 — verify + commit.** tsc 0-new; existing billing tests green. **Commit** `feat(billing): single $29/mo flat plan (unlimited workspaces) + 14-day trial`.

---

## Task 2: GMV fee helper (pure, TDD)

**Files:** Create `packages/crm/src/lib/billing/gmv.ts`; Test `packages/crm/tests/unit/billing/gmv.spec.ts`.

- [ ] **Step 1 — failing tests.** `GMV_FEE_PERCENT === 2`; `computeInvoiceApplicationFeeCents(0) === 0`; `computeInvoiceApplicationFeeCents(10_000) === 200` (2% of $100.00); `computeInvoiceApplicationFeeCents(12_345) === 247` (rounded); negative/NaN input → `0` (defensive).
- [ ] **Step 2 — run, see fail.** `cd packages/crm && node --import tsx --test tests/unit/billing/gmv.spec.ts` → FAIL (module missing).
- [ ] **Step 3 — implement.**
```ts
// packages/crm/src/lib/billing/gmv.ts
// The platform GMV fee taken on payments processed THROUGH SeldonFrame's
// Stripe Connect (the SMB's own sales). Sell outside SF → no fee.
export const GMV_FEE_PERCENT = 2;
export function computeInvoiceApplicationFeeCents(totalCents: number): number {
  if (!Number.isFinite(totalCents) || totalCents <= 0) return 0;
  return Math.round((totalCents * GMV_FEE_PERCENT) / 100);
}
```
- [ ] **Step 4 — pass + commit.** `feat(billing): GMV fee helper — 2% of SF-processed sales (TDD)`.

---

## Task 3: Apply the 2% at the three SMB-sales charge sites

**Files:** `packages/crm/src/lib/proposals/checkout.ts`, `packages/crm/src/lib/payments/providers/stripe.ts`.

- [ ] **Step 1 — proposals (subscription).** In `buildCheckoutSessionParams` (`lib/proposals/checkout.ts`), add to the `subscription_data` object: `application_fee_percent: GMV_FEE_PERCENT` (import from `lib/billing/gmv`). Update the existing proposals-checkout test to assert `subscription_data.application_fee_percent === 2`.
- [ ] **Step 2 — payments invoice (computed amount).** In `lib/payments/providers/stripe.ts` `createInvoice` (~116–145): before `stripe.invoices.create`, sum the item amounts already computed (`const totalCents = input.items.reduce((s, it) => s + Math.round(it.unitAmount * (it.quantity ?? 1) * 100), 0)`); pass `application_fee_amount: computeInvoiceApplicationFeeCents(totalCents)` into the `invoices.create` params (alongside the existing `{ stripeAccount }` option). Skip the field when the fee is 0 (Stripe rejects `application_fee_amount: 0` on some invoice shapes — only set it when `> 0`).
- [ ] **Step 3 — payments subscription (%).** In the same file's `createSubscription` (~205–217): add `application_fee_percent: GMV_FEE_PERCENT` to the `subscriptions.create` params.
- [ ] **Step 4 — FLAG the soul-marketplace.** Do NOT change `lib/marketplace/actions.ts:504` / `:678` (`application_fee_amount: 0`). Add a one-line code comment at each: `// GMV policy TBD for soul-marketplace sales — left at 0 (decide separately).` Note it in the report.
- [ ] **Step 5 — verify + commit.** Tests green (proposals params + gmv helper); tsc 0-new; `check-use-server` clean. **Commit** `feat(billing): take 2% GMV on proposals + invoices + subscriptions via Stripe Connect`.

---

## Task 4: Verify (DO NOT MERGE)

- [ ] Suites: `cd packages/crm && node --import tsx --test tests/unit/billing/*.spec.ts tests/unit/proposals/*.spec.ts` → green.
- [ ] `tsc` 0-new; `check-use-server` clean; **no migration** (env + code only).
- [ ] **Report:** the $29-flat + trial changes (file:line), the 3 GMV sites + the helper, the soul-marketplace flag, the regression statement (legacy tiers still resolve in the webhook; platform-subscription charges are NOT touched by the GMV fee — only connected-account/SMB-sales charges are), the new-test count, the tsc/test/check-use-server results, and **the explicit DO-NOT-MERGE + the Stripe-test-mode gate** (below).

## Stripe dashboard steps (Max — do these; the code reads them)
1. **Create the $29 plan** — Stripe → Products → New: "SeldonFrame" · **Recurring · $29.00/mo**. Copy the **price id** (`price_…`). *(Do it in TEST mode first.)*
2. **Set env** — `STRIPE_AGENCY_BASE_PRICE_ID=<that price id>` in Vercel (Preview/Prod) + local `.env`. (Reusing the existing var name — no rename needed.) Confirm `STRIPE_SECRET_KEY`, `STRIPE_BILLING_WEBHOOK_SECRET`, `STRIPE_CONNECT_WEBHOOK_SECRET` are set.
3. **Webhooks** (if not already): billing → `https://<app>/api/webhooks/stripe-billing` (events: `checkout.session.completed`, `customer.subscription.updated`, `customer.subscription.deleted`, `invoice.paid`, `invoice.payment_failed`); Connect → `https://<app>/api/webhooks/stripe/connect` (the connected-account events, listen "on connected accounts").
4. **The 2% GMV needs NO Stripe config** — it's a per-charge API parameter; it just requires the SMB to have completed Connect onboarding (already wired via the proposals Connect flow).
5. **Test mode gate (before merge):** run the upgrade checkout → confirm **14-day trial + $29** on the subscription; accept a test proposal on a connected test account → confirm the **2% application fee** appears on that payment in the connected account. Then merge.

## Self-Review
- Coverage: $29 flat (T1) ✓; unlimited (agency tier already) ✓; 14-day trial (T1) ✓; 2% GMV at all 3 SMB-sales sites (T3) + pure helper (T2) ✓; soul-marketplace flagged not assumed (T3.4) ✓; no migration ✓; do-not-merge + Stripe steps ✓.
- Risk notes: platform subscription (SF billing the SMB $29) must NOT carry the GMV fee — only the SMB's *own* connected-account sales do. The legacy builder/workspace tiers stay catalogued for webhook back-compat. A future cleanup can rename the internal "agency" tier to "flat" and simplify the plan-picker UI to the single plan.
