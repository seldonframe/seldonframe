# Plan — Record v3: single slot, agent-loop explainer, BYOK modal

Spec: `docs/superpowers/specs/2026-07-12-record-v3-design.md`. Worktree: `.claude/worktrees/record-redesign` (branch `feat/record-redesign` off origin/main). Commit per task. TDD where the surface is testable offline (jsdom specs import `tests/setup-dom.ts` first-line — see green-main lesson).

**Design reference (visual gate):** `docs/design/record-to-agent-handoff-v2/Record.dc.html` — Geist font, page `#0B0F0E`, cards `#0F1413`, text `#E7E5DE`, teal accent `#14B8A6` (hover `#2DD4BF`), muted `rgba(231,229,222,0.5)`, radius 12/14, step-strip + recap-panel structure. Desktop left-aligned, mobile centered.

## T1 — `LlmKeyDialog`: in-place BYOK modal (S4a)
**Files:** new `packages/crm/src/components/integrations/llm-key-dialog.tsx`; edit `studio/agents/[id]/editor-client.tsx:463-474`, `studio/agents/[id]/test-client.tsx:~183`, `studio/agents/[id]/lifecycle/run-evals.tsx:~174`.
- Dialog (from `components/ui/dialog.tsx`) wrapping `AnthropicKeyField` + `saveLlmKeyAction`; props `{ open, onOpenChange, onSaved }`; pending/error state inline (pattern: `components/billing/upgrade-modal.tsx:180-334`). Fail-soft: surface the action's error string in the dialog, never silently close.
- Replace the three `needs_byok` **Links** with a button that opens the dialog; `onSaved` re-triggers the blocked call (refine / test / evals) where a retry handler already exists, else just closes and clears the error.
- Tests: dialog renders field; save success calls `onSaved` + closes; save error stays open + shows message. (Mock the server action via DI prop — default `saveLlmKeyAction`.)

## T2 — Onboarding stamp on claimed compile (S4b — root fix for the `/clients/new` bounce)
**Files:** `packages/crm/src/app/api/v1/recordings/compile-agent/route.ts` + its lib (guards live in `lib/recordings/route-guards.ts` — route.ts exports ONLY handlers, L-31); reuse `markOperatorOnboarded` (see `claim-build`'s `link-owner/route.ts` for the call shape).
- When compile-agent runs for a **claimed** session (authed user + session claim path), stamp `markOperatorOnboarded` for that user/org — idempotent, soft-fail (log, never block compile).
- Test: claimed compile marks onboarded; unclaimed/anonymous compile does not; stamp failure doesn't fail the compile.

## T3 — Single-slot capture UI (S1 — the core)
**Files:** `record-client.tsx`, `record-ui/slot-card.tsx`, `record-ui/recap-panel.tsx`, new `record-ui/capture-card.tsx` + `record-ui/traced-list.tsx`. **No changes** to `recorder-machine.ts` transitions or APIs; the machine's 6 slots stay.
- Render logic: exactly ONE **capture card** visible (the first non-traced slot: empty/recording/uploading/compiling/failed states as today's slot-card states, restyled per design). Traced slots render as a compact stacked list (thumbnail, editable label, "Traced · N steps", re-record link).
- Edge-case prompting moves INTO the recap panel: once ≥1 slot traced, recap shows a "Make it trustworthy" row — Seldon-voiced prompt ("Anything ever go differently? Record that too.") + "+ Record an edge case" / "or upload" affordances that start capture on the next empty slot (same dispatch as today's per-slot buttons). Hide when all 6 used or one is busy.
- Keep restored-banner, step-strip, Ask-Seldon chat, claim CTA; restyle all to the design tokens above.
- Tests: with all slots empty → one capture card, no grid; after trace → traced list + edge-case prompt in recap; busy slot → prompt hidden; 6 traced → prompt gone.

## T4 — Agent-loop SVG explainer (S2a)
**Files:** new `record-ui/agent-loop-diagram.tsx`, placed under the hero in `record-client.tsx` (or `page.tsx` if it can be server-rendered — prefer server; it's static).
- Inline SVG, CSS-only animation (`@keyframes` in a `<style>` tag or Tailwind arbitrary — match repo idiom): 5 nodes **Trigger → Watch → Decide → Act → Check with you** on a rounded loop, a teal dot traveling the path, nodes brightening as the dot passes. `prefers-reduced-motion`: static diagram. Sized ~560px wide desktop, full-width mobile. One-line caption: "A recording becomes an agent that runs this loop for you."
- Test: renders (renderToString smoke) + has accessible title/desc.

## T5 — Rotating wait copy (S2b)
**Files:** new `record-ui/wait-copy.tsx`, used by capture-card during `uploading`/`compiling`.
- Cycle lines every ~2.5s (uploading set vs compiling set: "Reading your recording…", "Listening to your narration…", "Mapping the steps you took…", "Working out what's safe to automate…"). Real progress bar stays wired to actual progress — the copy is flavor, never a fake claim. Cross-fade or simple swap per repo idiom.
- Test: advances lines on timer (fake timers), pauses on unmount, honest set per status.

## T6 — Mobile centering + design pass (S3)
**Files:** `record-client.tsx`, `record-ui/*`, `page.tsx`.
- `<720px`: centered hero + step strip, single column, full-width capture card, sticky bottom claim CTA when recap ready. Desktop: left-aligned per design. Verify no horizontal scroll.
- Final visual sweep against `Record.dc.html` (header with `/record` mono breadcrumb, footer privacy line "Recordings stay private — they train your agent only.").

## Verify (controller-run, not the implementer)
- `/verify-build` (worktree) — tests + tsc + check-use-server + migration-journal (no migrations expected) + regression-grep.
- `vision-verify`: screenshot /record desktop + mobile vs the design; grade with vision-grader.
- LOC calibration: UI composition ≈0.94x tests; T3 has render-branch logic → budget ~1.2x. Total est. ~900-1,300 LOC incl. tests.

## Worktree test method
`packages/crm/node_modules` junction from the main repo per the worktree-typecheck memory (PowerShell `New-Item -ItemType Junction`); re-verify the junction exists before running; judge failures by delta vs baseline; remove junction after.
